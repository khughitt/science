# Test artifacts generated by render/number/staleness tests run
# directly against fixture directories. Mirrors mm30/.gitignore.
# The `.dot.reference` files (when present) are checked in as
# byte-identity anchors; everything else is regenerated per-run.
*/doc/figures/dags/*-auto.dot
*/doc/figures/dags/*-auto.png
*/doc/figures/dags/*-numbered.dot
*/doc/figures/dags/*-numbered.png
*/doc/figures/dags/.audit-unpropagated-*.md
