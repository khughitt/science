{"file": "entities/claims.md", "line": 25, "id": "v1", "expected_outcome": "verified", "reason": "bound to summary.feather column=score (single row, value 0.978); prose '0.978' has quantum 0.001, so its displayed-precision range (0.9775, 0.9785) strictly contains the artifact value -- verified."}
{"file": "entities/claims.md", "line": 27, "id": "m1", "expected_outcome": "mismatch", "reason": "bound to per_disease.feather column=score where disease=MESH:D009101 (value 0.42); prose '0.50' has range (0.495, 0.505), which excludes 0.42 -- mismatch, not a silent pass."}
{"file": "entities/claims.md", "line": 29, "id": "o1", "expected_outcome": "unverifiable", "reason": "opaque locator on results.json, which exists -- resolution succeeds (existence-checked, content=False) but the value is never read, so the outcome is unverifiable, never a silent pass."}
{"file": "entities/claims.md", "line": 31, "id": "e1", "expected_outcome": "error", "reason": "pointer locator on missing-artifact.json, which does not exist under project_root or data_root -- resolve_artifact fails closed with an error, never a silent unverifiable."}
{"file": "entities/claims.md", "line": 33, "id": "o2", "expected_outcome": "error", "reason": "opaque locator on missing-opaque.dat, which does not exist -- artifact resolution runs BEFORE the opaque short-circuit, so a missing opaque artifact is an error, not unverifiable (proves opaque does not skip existence-checking)."}
{"file": "entities/claims.md", "line": 35, "id": "a1", "expected_outcome": "error", "reason": "column locator on per_disease.feather with where={disease: DUP}, which matches 2 rows (a deliberately duplicated key) -- read_scalar requires exactly one matching row, so this is an error, not an arbitrary pick of the first match."}
{"file": "entities/percent-and-binding-errors.md", "line": 16, "id": "p1", "expected_outcome": "unverifiable", "reason": "prose literal '13%' carries a %-unit -- scale normalization between a displayed percentage and a raw artifact fraction is deferred (out of scope), so the value is never read regardless of the bound row -- unverifiable by design, not a bug."}
{"file": "entities/percent-and-binding-errors.md", "line": 1, "id": "orphan1", "expected_outcome": "error", "reason": "declared in numeric_claims frontmatter but has zero [^orphan1] markers anywhere in the body -- a BindingError with no line context, so it surfaces at the default line 1 (a claim that is never pinned to prose must not silently vanish)."}
{"file": "entities/percent-and-binding-errors.md", "line": 20, "id": "dup1", "expected_outcome": "error", "reason": "declared once but pinned by two separate [^dup1] markers in the body -- ambiguous which prose token the claim refers to, so it is a BindingError at the first occurrence's line, not a silent pick of either one."}
