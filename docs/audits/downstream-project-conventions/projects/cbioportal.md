# Downstream Project Audit: cbioportal

## Header

- **Project root:** `/home/keith/d/r/cbioportal`
- **Project SHA at audit:** `553d7cc4774a5293b1ff54fb308e54cd022beeee` (inventory pin; HEAD has since advanced to `e7f4c7e5`)
- **Dirty tree summary:** 2 modified, 0 untracked at inventory time (` M code/scripts/combine_samples_tmb.py`, ` M code/scripts/prepare_dndscv_input.py`); HEAD has since advanced — see Notes
- **Inventory artifact:** `docs/audits/downstream-project-conventions/inventory/cbioportal.json`
- **Science SHA used for comparators:** `a059f7d3dd46fb197df14339e6e9576bbb5615dd`
- **Auditor / agent:** Claude Opus 4.7 (subagent, audit Phase 2)
- **Audit date:** 2026-04-25

## Known Starting Conditions

- `validate.sh` is being migrated to a managed artifact per the MAV plan; observed local modifications (none — see §8) are recorded as evidence for the MAV artifact list, not drift.
- `archive/` is `.gitignore`d and not tracked; AGENTS.md describes it as "superseded material (old EDA, study-specific scratch, legacy R artifacts)". The inventory does not surface it because v0 only lists tracked top-level entries plus a fixed set of present-untracked dirs — see Notes for §10.
- The pinned project SHA differs from current HEAD; six commits have been added since (`fix(t131)…` → … → `docs(papers): research Kadelka2026`). All path/line citations below are valid as of the pinned SHA.

---

## 1. Project Center Of Gravity

- **Primary shape:** hybrid — research + pipeline. `code/` (140 tracked files) and `doc/` (147 tracked files) are roughly tied as the dominant spines; the project is a Snakemake-driven analysis pipeline (`code/workflows/Snakefile`, configs, R+Python scripts) wrapped around an actively-curated research framework (questions, hypotheses inside `specs/research-question.md`, papers, topics, plans, interpretations, pre-registrations).
- **Dominant tracked directories:** `doc/` (147), `code/` (140; with 122 scripts + 58 tests), `data/` (27 reference TSVs/BEDs/xlsx), `tasks/` (2 files), `specs/` (1 file).
- **Dominant untracked / symlinked directories:** `data/` (untracked payload — genie/, mc3.maf.gz, regenerable t111/t121 derivatives — alongside the 27 tracked reference files), `results/` (gitignored pipeline outputs), `models/` (gitignored, currently empty), `.snakemake/`, `.venv/`, `node_modules/`, `.worktrees/`. One symlink: `science -> /mnt/ssd/Dropbox/science` (a convenience for `pyproject.toml`'s relative `science-tool` editable path; see `.gitignore`).
- **`science.yaml` profile / aspects:** `profile: research`; `aspects: [computational-analysis, software-development]`; `ontologies: [biology]`; `layout_version: 2`.
- **Does profile + aspects capture the real project?** **partial.** The single `profile: research` flattens what is in practice a pipeline-heavy project that ships a Snakemake DAG, an R/Python codebase with 58 tests, multi-config runs, and a viewer-adjacent `doc/guides/canonical-outputs.md` describing consumer-facing feathers. The two-aspect `[computational-analysis, software-development]` is the most honest signal that the profile is too narrow; AGENTS.md "Active Roots" reinforces this by describing `code/workflows/`, `code/scripts/`, `code/config/`, `data/`, `results/`, `models/` first-class.
- **Multi-axis needed?** **yes.** The shape is closer to *research* + *pipeline* (or *research* + *software*) than to a single profile. If the profile system added a `pipeline` axis or accepted compound profiles, this project would naturally consume it.

Compared to peer inventories (peeked at, not deep-synthesised): cbioportal is the smallest (15 long-form files vs many more for protein-landscape/mm30) but is *not* content-leaning — it sits closer to the pipeline corner than to a viewer/app or content shape. Classification: **valid project-specific shape; profile capture gap is a recurring candidate convention** (worth flagging because the same `[computational-analysis, software-development]` aspects pair likely recurs).

## 2. File And Directory Organization

- **`doc/` taxonomy and naming:** `doc/{audits,background/{papers,topics},datasets,discussions,guides/{*,modalities/},interpretations,meta/{*,curation/},methods,papers,plans,questions,reports,searches,topics}`. Mostly date-prefixed filenames inside `discussions/`, `interpretations/`, `meta/next-steps-*`, `meta/curation/`, `plans/`, `searches/`. Questions use `qNNN-…` slugs. Papers use `AuthorYear.md`. — *recurring candidate convention* (the `doc/{questions,papers,topics,plans,searches,…}` shape matches peer projects).
- **`docs/` vs `doc/`:** Only `doc/` (singular) used. `docs/` does not exist. — *recurring candidate convention* (consistent with the Science template).
- **`specs/` usage:** One file: `specs/research-question.md` (`spec:research-question`). Used for the canonical research question + scope + hypotheses + assumptions. — *recurring candidate convention*.
- **`tasks/` layout and archival:** `tasks/active.md` (385 lines, 35 active tasks at audit time) + `tasks/done/2026-04.md` (1196 lines, 102 archived tasks for the month). Markdown-section-per-task format with `## [tNNN] Title`. — *recurring candidate convention* (matches the Science template's task lifecycle).
- **`knowledge/` (generated vs source):** `knowledge/{graph.trig, reports/kg-migration-audit.json, sources/local/{manifest.yaml, entities.yaml, relations.yaml, mappings.yaml}}`. The `sources/local/manifest.yaml` registers six project-local entity kinds (`meta`, `pre-registration`, `synthesis`, `guide`, `modality-guide`, `curation-sweep`) under `layer: layer/local`, `strictness: typed-extension`. — *project-grown convention that is healthy* — this is exactly the surface the curation sweep at `doc/meta/curation/curation-sweep-2026-04-22.md` flagged as a recurring tooling gap, and the project has wired it correctly.
- **Code locations:** `code/{config, envs, notebooks, scripts, workflows}`. `scripts/` (122) is the bulk — Python plus one `summary.Rmd`, three `.R` (`run_dndscv.R`, `run_select.R`, `run_select_pathway_aggregated.R`, `preflight_select_env.R`) and one shell (`validate_select_run.sh`). `notebooks/` holds three marimo `.py` files plus `viz.py` plus its own nested `pyproject.toml + uv.lock` (hermetic notebook env). `workflows/Snakefile` is the single workflow root. — *valid project-specific convention* (`code/` instead of `src/` is consistent across peer projects per the inventory comparator note); the nested `code/notebooks/pyproject.toml` is *project-specific specialization*.
- **Data / results / log / model locations:** Tracked `data/` holds reference + small datasets and per-payload `data/genie/` is gitignored; `results/`, `models/`, `logs/`, `.snakemake/`, `.venv/` all gitignored. Two distinct README rows: `data/README.md` for reference data + provenance, `data/external/README.md` for vendored upstream packages (`select_v1.6.4.tar.gz`), `data/panels/README.md` for the IMPACT/F1 BEDs. — *recurring candidate convention* (descriptor README per data-class).
- **Archive and migration directories:** `archive/` exists at the project root and is `.gitignore`d (entries: `data_pkg_todo.md`, `eda/`, `extra/`, `genie.rgz`, `msk_ch_2023/`, `msk_met_2021/`, `unused.py`, `unused.snakemake`). AGENTS.md documents it as "superseded material". Because it is gitignored, the v0 inventory misses it — *stale or unclear convention* worth surfacing: it conflicts with the project's own "no legacy/compatibility layers" rule (AGENTS.md) and with the audit-tooling assumption that everything-relevant is tracked. *Recurring across peer projects?* — partly: the inventory comparator was not run against peers for this directory specifically; flag for synthesis.

## 3. Entity Model And Metadata

Sampling floor: `min(5, all)` per family + every long-form (>300 lines) and every embedded-metadata-candidate file. The inventory enumerates 9 embedded-metadata candidates and 15 long-form files; samples below quote each family's frontmatter shape from real reads.

- **Hypotheses:** *no separate hypothesis family.* The single hypothesis is embedded as a numbered list inside `specs/research-question.md` (lines 39-42). — **missing entity family** that is clearly present in prose; would benefit from being upgraded to a dedicated `hypothesis:` family if the project continues to grow.
- **Propositions / claims:** N/A — no proposition/claim family present. Verdicts live inline inside interpretations (`## Verdict` heading + bracket polarity tokens `[~]`/`[+]`/`[?]` — see `doc/plans/2026-04-19-verdict-token-backfill-audit.tsv` which catalogs these).
- **Questions:** 12 files in `doc/questions/` (`q001`-`q012`). Sampled `q003`, `q009`. Frontmatter: `id`, `type: question`, `title`, `status`, `revisit_condition` (q009 only), `ontology_terms[]`, `datasets[]`, `source_refs[]`, `related[]`, `created`, `updated`. Bodies follow a `# Title / ## Summary / ## Why It Matters / ## Current Evidence / ## Thoughts` shape consistently. — *recurring candidate convention*. q009 carries an inline status note above the body that re-states the deferred verdict with a revisit condition — *embedded provenance*, useful pattern.
- **Papers:** 58 paper entries split across **two locations**: 49 in `doc/background/papers/` (foundational, per AGENTS.md / pipeline-roadmap framing) and 19 in `doc/papers/` (newer / research-front, including some canalization papers added 2026-04-25). Same frontmatter shape (`id: "paper:AuthorYear"`, `type: paper`, `title`, `status`, `ontology_terms[]`, `datasets[]`, `source_refs[]`, `related[]`, `created`, `updated`). — *unclear convention*: the split is intentional per AGENTS.md but the boundary is fuzzy (e.g., `Yaacov2023` lives under `doc/papers/`, but is heavily cited from `q009` and the t126 pre-reg, which would qualify it as foundational). Recommend Phase 3 ask whether the split is a useful axis (`background` vs `front-of-mind`) or accidental drift.
- **Topics / background:** 17 topic files: 16 in `doc/background/topics/` + 1 in `doc/topics/` (`signature-decomposition-unmatched-normal.md`, 366 lines). Same frontmatter shape (`id: "topic:…"`). — *unclear convention*: the same `background/` vs flat split as papers. The flat-`doc/topics/` topic is one of the longest doc files in the project, with seven embedded-metadata candidate sections (lines 53/95/166/232/260/324/352).
- **Datasets:** 2 dataset files: `doc/datasets/replication-timing-constitutive-regions.md`, `doc/datasets/normal-tissue-spectra.md`. Frontmatter: `id: "dataset:…"`, `type: dataset`, `status`, `source_refs[]`, `related[]`. Body uses a markdown table for source provenance (DOI, assembly, supplementary URL, SHA256, retrieved date) plus `## File shape`, `## Acquisition steps`, etc. — **strong project-grown pattern** for dataset descriptor: the SHA256-pinned + DOI-pinned + assembly-pinned table is the kind of provenance Science doesn't currently model formally (no `datapackage.json` files exist; `datapackages: []` in inventory).
- **Interpretations:** 8 files in `doc/interpretations/`. Sampled `2026-04-22-t122-rt-brca-pilot.md`. Frontmatter introduces project-specific keys: `mode: research`, `input: "<run cohort string>"`, `workflow_run: "t122-2026-04-22-brca-rt-pilot"`, `prior_interpretations[]`. Body uses a structured `## Verdict / ## Run Surface / ## Main Result / …` shape. **One stale outlier:** `doc/interpretations/2026-04-18-t070-poc-comparison.md` has no frontmatter (called out in the curation sweep, line 33). — *recurring candidate convention with one drift instance*; the `mode`, `input`, `workflow_run`, `prior_interpretations` fields are project-grown and worth a Phase-3 look.
- **Reports / syntheses:** 1 synthesis file: `doc/papers/synthesis-2026-04-18-somatic-mutations-in-normal-tissue.md` (`type: synthesis`). Lives under `doc/papers/`, not under a dedicated `reports/` or `syntheses/`. `doc/reports/` exists but is empty. — *unclear convention*; the project-local `synthesis` entity kind is registered in `knowledge/sources/local/manifest.yaml` so the type is legitimate, but the placement under `doc/papers/` is surprising.
- **Plans / pre-registrations:** 15 files in `doc/plans/` mixing implementation plans, designs, design-reviews, plan-reviews; 2 pre-registrations live separately under `doc/meta/pre-registration-tNNN-…md`. The pre-reg path is a known *recurring candidate convention* across the four downstream projects (the plan calls it out under "doc/meta/pre-registration-*"). — Plans are *recurring candidate convention*; the `plans/<date>-tNNN-…-{design,plan,implementation-plan,design-review,plan-review}.md` naming is consistent. **Outliers:** (a) `doc/plans/t111-data-gate-record.md` has no frontmatter (drift); (b) `doc/plans/2026-04-19-verdict-token-backfill-audit.tsv` is a TSV (not markdown) sitting in `plans/` — drift or specialization, depending on whether tabular plans are an intended pattern.
- **Tasks:** see §5.
- **Project-specific entities:**
  - `meta:next-steps-YYYY-MM-DD` — recurring next-steps/gap-analysis docs in `doc/meta/`.
  - `meta:pre-registration-tNNN-…` — see plans/pre-reg above.
  - `curation-sweep:YYYY-MM-DD` — registered project-locally; one instance.
  - `guide:…`, `modality-guide:…` — project-grown registered local kinds. Five modality guides (panel, WES, cross-study aggregation, driver detection, variant annotation) under `doc/guides/modalities/` plus two top-level guides (`audit-process.md`, `canonical-outputs.md`). The audit-process guide explicitly cites mm30 as the originating template (`adapted from the MM30 project (~/d/r/mm30/doc/guides/audit-process.md)`) — strong recurring-cross-project signal.
  - `discussion:YYYY-MM-DD-…` (with `focus_type` + `focus_ref` fields) — three discussions, project-grown shape.
  - `review:YYYY-MM-DD-…` — single instance (`doc/plans/2026-04-24-t131-dndscv-three-way-comparison-design-review.md`); has its own `plan:` field pointing at the reviewed plan + an `overall: WARN` enum. Project-grown.
  - `handoff` — `doc/meta/handoff-2026-04-25-t131-dndscv.md` — no frontmatter, not a registered kind.
  - `bias-audit` — `doc/meta/bias-audit-cross-study-aggregation-pipeline.md` filed as `type: task`. Misclassified; shape resembles an audit/synthesis more than a task.

Cross-cutting observations:

- **Missing entity families (clearly present in prose or paths):** `hypothesis` (one hypothesis embedded inside `specs/research-question.md` lines 39-42); `proposition`/`claim` (verdict polarity tokens `[+]/[~]/[?]` documented in `doc/plans/2026-04-19-verdict-token-backfill-audit.tsv` but not lifted to a typed entity); `report` (directory exists empty, syntheses live elsewhere); `handoff` (one instance, no frontmatter).
- **Duplicated information across entity types:** `specs/research-question.md` carries hypothesis + assumptions + scope. `science.yaml` independently carries `research_question`, `scope.in_scope`, `scope.out_of_scope`. The two are aligned in content but maintained separately — drift surface.
- **Inconsistent `id` prefixes:** None observed for the **enumerated** types; every parsed entity follows `<type>:<slug>` exactly. **Inventory anomaly:** `entity_id_prefix_counts: {"<unparseable>": 124}` in the inventory is contradicted by `entity_id_paths` immediately below it (which contains 124 fully-parsed `<prefix>:<slug>` entries). Flag as v0 inventory bug.
- **Overloaded `type` / `status` / `phase` values:** `status` enum spans `abstract-read`, `active`, `deferred`, `draft`, `in-progress`, `proposed`, `read`, `summarized`, `unread` (9 values, partly cross-cutting reading-state vs work-state); paper-level `status: read|abstract-read|summarized|unread|active` is a reading-progress sub-axis that overloads the generic doc-level `status` axis. `phase` is unobserved (consistent with the hypothesis-phase plan being upstream-only at this stage). `type` enum is reasonable (16 values) but mixes Science-canonical (`paper`, `question`, `topic`, `dataset`, `plan`, `task`) with project-local (`meta`, `synthesis`, `guide`, `modality-guide`, `curation-sweep`, `pre-registration`, `discussion`, `interpretation`, `review`).
- **Fields that should likely be enums:** `status` (especially the reading-state half — `read|abstract-read|summarized|unread`); `priority` (`P1`/`P2`/`P3` already enum-shaped in tasks); `mode` (interpretation files use `research`/`standard`); `overall` (`WARN`/`PASS`/`FAIL` in review files).
- **Fields that should likely be structured objects, not strings:** `input` in interpretation frontmatter (`"BRCA pair: tcga_mc3 vs msk_impact_2017"`) is a free-text run-cohort descriptor that would parse cleanly into `{cohort_pairs: [{matched, unmatched, cancer_type}]}`. `workflow_run` (`"t122-2026-04-22-brca-rt-pilot"`) packs a task-id + date + slug. Dataset descriptor table rows (`source / paper / DOI / assembly / supplementary URL / SHA256 / retrieved`) would benefit from being a YAML sequence rather than a free-form markdown table.
- **Inline information that should likely be metadata:** Verdict polarity tokens `[+]/[~]/[?]/[-]` and confidence floats inside interpretation prose (the `verdict-token-backfill-audit.tsv` tabulates them); these are post-hoc structured. Reading-state on papers (currently a `status` value) is conceptually a separate axis from work-status.
- **Embedded entity metadata blocks inside long prose / plans:** Inventory flags 9 candidate files. The two largest plan files (`t078-select-cooccurrence-implementation-plan.md` 4928 lines / 13 candidate starts; `t111-normal-tissue-spectra-plan.md` 2335 lines / 9 candidate starts) interleave per-task metadata (`Files: / Step 1: / Test runner: / Commits:`) for ~20 numbered tasks per plan. These look like tasks-shaped sub-entities embedded in a parent plan rather than true frontmatter blocks; manual sampling of `t078` Tasks 1-3 confirms each Task uses a key:value pseudo-block but not `---` fences. *Confidence: low for "true frontmatter"; high for "structured task entries that could be lifted to typed sub-entities"*. The inventory's lexical heuristic is correct that something structured is present.
- **Inconsistent use of `related` / `datasets` / `source_refs` / `datapackage` / `local_path` / `consumed_by` / `produces`:** `related[]` is present on 107 of 123 frontmattered docs and is the project's catch-all link list. `source_refs[]` on 103, `datasets[]` on 71. `datapackage`, `local_path`, `consumed_by`, `produces` are unused (0 hits — `datapackages: []` in inventory). The `t128` task ("Emit retroactive `datapackage.json` manifests for results/poc-2026-04-17/ and results/signature-brca-2026-04-22/") explicitly recognizes this gap. `datasets[]` mixes two semantics: list-of-strings used as data-source names (`"PCAWG"`, `"cBioPortal studies"`) and one occurrence (`paper:Bailey2018.md`) using string ids (`"tcga-pancanatlas"`).
- **External accession / ontology usage:** PMIDs and DOIs appear inside paper bodies (e.g., `Bailey2018.md` line "PMID: 30096302", `Gao2023.md` "DOI: 10.1038/s41588-023-01537-1"). HGNC and OncoTree are mentioned as **gaps** (active tasks `t082` HGNC alias mapping, `t083` OncoTree mapping). UBERON appears as a *file* (`data/tissue_uberon_mapping.tsv`) but no UBERON ids inside frontmatter. `ontology_terms[]` is present (87 docs) but values are free-text natural-language tags (e.g., "mutational signatures", "replication timing") rather than ontology-anchored CURIEs. — *recurring candidate convention with under-formalization*.

## 4. Planning, Pre-Registration, And Decision Records

- **Implementation plans:** `doc/plans/<YYYY-MM-DD>-tNNN-<slug>-{plan,implementation-plan}.md`. Frontmatter: `id: plan:…`, `type: plan`, `status`, `related[]` (commonly to `task:tNNN` and the matching design). Examples: `2026-04-22-t077-glmm-logit-plan.md`, `2026-04-25-t078-select-cooccurrence-implementation-plan.md`, `2026-04-22-t076-nan-vs-0-implementation-plan.md`. Most include the *"REQUIRED SUB-SKILL: Use superpowers:executing-plans / subagent-driven-development"* directive at the top — project-grown convention.
- **Design / spec docs:** `doc/plans/<YYYY-MM-DD>-tNNN-<slug>-design.md` paired with the implementation plan. Two examples: `2026-04-18-t070-msk-impact-panel-version-drift-design.md` + `…-implementation-plan.md`; `2026-04-24-t131-dndscv-three-way-comparison-design.md` + `…-design-review.md`. The single `specs/research-question.md` is the only `specs/` artifact (rest are in `doc/plans/`).
- **Pre-registrations:** `doc/meta/pre-registration-tNNN-<slug>.md`. Two instances: `pre-registration-t077-glmm-logit-pooling.md` (365 lines), `pre-registration-t126-sbs1-lrr-bias-test.md`. Frontmatter `type: pre-registration` (project-local kind). Bodies include explicit decision rules + locked thresholds. — *recurring candidate convention*; placement matches the plan's "doc/meta/pre-registration-*" expectation.
- **Next-step / gap-analysis docs:** `doc/meta/next-steps-YYYY-MM-DD.md`. Four instances (2026-04-14/17/22/24). `type: meta`. Each is structured as `## Recent Progress / ## Current State / ## P1 tasks / ## Blocked Tasks / ## Open decisions / ## Next-up suggestions` — consistent shape.
- **Curation sweeps:** `doc/meta/curation/curation-sweep-YYYY-MM-DD.md`. One instance. Frontmatter includes `generated_at`, `source_commit`, `scope`, `sweep_scope`, `since`, `mode`, `applied_changes`, `pending_decisions` — strong machine-readable shape.
- **Handoffs:** `doc/meta/handoff-YYYY-MM-DD-<slug>.md`. One instance (`handoff-2026-04-25-t131-dndscv.md`); **no frontmatter** — drift from the project's own metadata norms. Body is well-structured (`## What we're working on / ## Companion follow-up tasks already filed / …`).
- **Audit reports:** `doc/audits/<YYYY-MM-DD>-<slug>-audit.md`. One instance (`2026-04-13-create-combined-gene-cancer-freq-table-audit.md`, 346 lines). **No frontmatter** at the top of the file (uses `*As-of: 2026-04-13*` italicized header instead) — drift; bias-audit-style content also lives at `doc/meta/bias-audit-cross-study-aggregation-pipeline.md` (`type: task`) which suggests inconsistent placement of audit content.
- **Project roadmap / research plan files:** `doc/meta/pipeline-roadmap-2026-04-13.md` (`type: meta`). Single roadmap; aligned with `specs/research-question.md`.

## 5. Task Lifecycle

- **`tasks/active.md` shape:** Markdown sections per task: `## [tNNN] Title`, then a property list (`- priority: …`, `- status: …`, `- aspects: …`, `- group: …`, `- related: …`, `- blocked-by: …`, `- created: YYYY-MM-DD`), then a free-form prose block. 35 active tasks at audit time, 385 lines.
- **Done archive pattern:** `tasks/done/YYYY-MM.md` (one file per month). 102 tasks in `2026-04.md`. Same per-task shape, with `- completed: YYYY-MM-DD` added.
- **Completed tasks remaining in active files:** No — completed tasks are moved to `done/`. (Spot-checked: every `## [tNNN]` in active.md has `status: proposed` or `deferred`; no `done`/`retired`.)
- **Task ID format:** `tNNN` (3-digit zero-padded counter, currently `t025`-`t137`). One non-numeric special task ID surfaced: `task:bias-audit-cross-study-aggregation-pipeline` (`doc/meta/bias-audit-cross-study-aggregation-pipeline.md`) — slug-as-id, breaks the `tNNN` convention.
- **Status values observed:** Active: `proposed`, `deferred` only. Done: `done`, `retired` (sampled).
- **Use of `priority` / `aspects` / `group` / `related` / `blocked-by` / `created` / `completed`:** All used. Priority enum: `P1`, `P2`, `P3` (no P0 in active). Aspects: `[computational-analysis]`, `[software-development]`, sometimes both. Groups (active): `audit-fixes`, `audits`, `meta`, `meta-analysis`, `pipeline`, `questions`, `searches` — 7 stable values, suggesting the group axis carries real signal. `related[]` includes mixed CURIE-like ids (`task:tNNN`, `paper:Year`, `question:qNNN-…`, `discussion:…`, `interpretation:…`).
- **Code, data, reports, interpretations link back to task IDs:** **partial.** Task IDs appear in: (a) interpretation frontmatter (`workflow_run: "t122-2026-04-22-brca-rt-pilot"`), (b) plan filenames (`<date>-tNNN-…`), (c) commit subjects (`fix(t131): thread random_seed…`), (d) `doc/meta/handoff-…`, (e) `doc/plans/2026-04-19-verdict-token-backfill-audit.tsv`. **Not** linked: code/script frontmatter (scripts have no metadata), data files, results files (no descriptors).
- **Are task groups a stable concept worth modeling:** **yes.** Seven values, used consistently, semantically distinct (search/audit/pipeline/meta-analysis are real workflow lanes). Worth a Phase-3 cross-project comparison.
- **Do tasks contain enough provenance, or should they link to result entities:** **partial.** Tasks describe *what* and *why* well. Provenance — *which feather, which commit, which run* — lives in interpretation files, plan-review files, handoffs, and the pipeline roadmap. Interpretation files are effectively the project's "result entities"; they are linked from tasks via `related[]` but the back-link is weak (interpretations link to tasks via `related` but tasks rarely list their canonical interpretation explicitly).

## 6. Code, Workflows, And Tests

- **Code root convention:** `code/` (not `src/`) — *recurring candidate convention* across peers per the plan's text.
- **Snakemake layout:** Single `code/workflows/Snakefile` (no rule-file split, no `rules/` directory). Reads `data_dir` and `out_dir` from a config file. Uses `script:` directives pointing at `../scripts/*.py`. The Snakefile sources several optional modules (e.g., `t078 SELECT` rules behind a `select:enabled` config flag) inline — *valid project-specific convention*.
- **Script naming and grouping:** Verb-prefix style (`build_*`, `compute_*`, `compare_*`, `create_*`, `aggregate_*`, `annotate_*`, `process_*`, `prepare_*`, `extract_*`, `convert_*`, `download_*`, `filter_*`, `fit_*`, `run_*`, `detect_*`, `cluster_*`). 122 scripts; the verb prefix carries pipeline-stage signal informally.
- **Notebook placement:** `code/notebooks/` with three task-tagged marimo notebooks (`q011_length_adjustment_topn_comparison.py`, `t070_poc_comparison.py`, `t131_three_way_ranking_comparison.py`) plus `viz.py` shared module. Has its own `pyproject.toml + uv.lock` — hermetic notebook env. — *project-specific convention*.
- **Tests colocated vs root:** `code/scripts/tests/` with 58 tracked test files + `conftest.py`, `fixtures/`, `data/select_toy/` integration fixture, plus `check_known_biology.py` (a non-pytest biological positive-control script). One R test (`test_run_select.R`). Tests-next-to-scripts convention.
- **Generated model / schema code:** None observed (no generated package, no schemas dir). Compare to peer projects — flag for cross-project comparison.
- **Viewer / app subprojects:** None. The Rmd-driven `summary.html` rule is commented out in the Snakefile (planned port to Python/marimo). `package.json` + `package-lock.json` + `commitlint.config.mjs` + `.husky/commit-msg` exist at the root for commit linting only — not a viewer.
- **Config and environment files:** `code/config/*.yml` (8 pipeline run configs); `code/envs/*.yml` (3 conda envs: `dndscv.yml`, `r-meta.yml`, `select.yml`); root `pyproject.toml + uv.lock`; `code/notebooks/pyproject.toml + uv.lock`; `.python-version`; `.env`.
- **Linkage from code artifacts to tasks / questions / hypotheses / datasets / plans:** **weak.** No frontmatter or sidecar manifests on script files. Some scripts have docstrings referencing task IDs in commit subjects, but discoverability runs through plan filenames + `related[]` in plans, not from the script side.
- **Would code artifacts benefit from lightweight metadata blocks or sidecar manifests?** **case-by-case.** Pipeline rules are already discoverable from the Snakefile + plan files; adding metadata to all 122 scripts would be costly. The marimo notebooks (3 task-tagged files) are a more attractive target — a `# task: t070` comment block (or a sidecar `*.md`) would close the gap from notebook → task without high migration cost. The most valuable sidecar surface is **per-result manifests** (the t128 task) — `datapackage.json` next to each `results/<run>/` directory.

## 7. Data, Results, And Provenance

- **Input data:** Mixed. **Tracked small public reference data:** 27 files in `data/` (gene mappings `grch37.tsv` / `grch38.tsv`, panel BEDs, COSMIC CGC, Bailey TableS1 TSV, Sanchez-Vega xlsx, UniProt protein lengths, tissue→UBERON mapping, cBioPortal study cohort profiles). **Ignored raw payload:** `data/genie/`, `data/mc3.v0.2.8.PUBLIC.maf.gz` (~720 MB), `data/li2021_somatic_mutations.tsv`, `data/Supp-RT-human-50kb-bins.xlsx`, `data/41586_2021_3836_MOESM5_ESM.xlsx` — gitignored, regenerable from public sources documented in `data/README.md` + per-step `doc/plans/t111-data-gate-record.md`. **Vendored upstream package:** `data/external/select_v1.6.4.tar.gz` (committed, with `data/external/README.md` for provenance).
- **Protected / manual data:** AACR GENIE (Synapse + DUA, manual download). Documented in AGENTS.md "External Reference Datasets" + `science.yaml.data_sources[1]` + `doc/plans/t111-data-gate-record.md` (which formalizes the data-access gate as its own record artifact). — *protected/manual data requiring explicit instructions*; documented well.
- **Public downloadable data:** cBioPortal study downloads via `download_study.py`, MC3 from GDC, Bailey/Sanchez-Vega/Yaacov2023/Dileep2015/Li2021 supplements via direct URLs in README + dataset descriptors with SHA256.
- **Intermediate outputs:** `results/<run>/studies/<id>/…` per-study and `results/<run>/summary/…` cross-study, all under gitignored `results/`.
- **Final results:** `results/<run>/summary/mut/table/{gene_cancer_study_annotated.feather, gene_cancer_study_ratio_annotated.feather}` are the documented "canonical consumer outputs" (per `doc/guides/canonical-outputs.md`). Ratios + counts + Bailey/CGC/Sanchez-Vega/CH overlays joined.
- **Logs:** `logs/` exists at root, gitignored, currently empty — *unclear convention* (intent is set up but not yet exercised).
- **Models:** `models/` exists at root, gitignored, empty.
- **Descriptors / sidecars:** `data/README.md`, `data/external/README.md`, `data/panels/README.md`, `data/cosmic_cgc.README.md` — three+1 README descriptors for tracked data subsets. Two formal `dataset:` entities under `doc/datasets/` for two analytic-pipeline-internal datasets.
- **Datapackages:** **none** (`datapackages: []` in inventory). The `t128` active task explicitly schedules retroactive `datapackage.json` emission for two existing `results/` runs — *project-recognized gap*.
- **Symlinked external storage:** One symlink: `science -> /mnt/ssd/Dropbox/science`. No `data` / `results` / `models` symlinks.
- **`.gitignore` exceptions:** None observed (no negation patterns); the `.gitignore` is straightforward.

Pattern classification:

- *Tracked source metadata only:* AGENTS.md "External Reference Datasets", `doc/plans/t111-data-gate-record.md`.
- *Tracked small public data:* the 27 files in `data/`.
- *Ignored raw payload with tracked descriptor:* `data/genie/`, `data/mc3.v0.2.8.PUBLIC.maf.gz`, `data/li2021_somatic_mutations.tsv`, `data/Supp-RT-human-50kb-bins.xlsx`, `data/41586_2021_3836_MOESM5_ESM.xlsx` (descriptor in README + dataset entities + t111-data-gate record).
- *Ignored generated output:* `results/`, `models/`, `logs/`.
- *Symlinked external data root:* none (only the `science -> …` convenience symlink, not a data root).
- *Protected/manual data requiring explicit instructions:* AACR GENIE (well-documented).
- *Unclear or risky tracking policy:* `archive/` (gitignored, undocumented in detail beyond AGENTS.md one-liner; see §2).

- **Is source / provenance visible?** **partial.** Strong on tracked-reference and ignored-raw with descriptors; gap on results provenance (no datapackage manifests; t128 acknowledges).
- **Can publicly available data be reproduced through a pipeline?** **partial.** The pipeline reproduces all *intermediate and final* analytic outputs from inputs. The *inputs themselves* are mostly auto-fetched (cBioPortal `download_study.py`); GENIE and a few manually-staged supplements break the loop. No rule reconstructs `data/external/select_v1.6.4.tar.gz` (vendored), but the t078 plan's Task 1 documents a `fetch_select_tarball` rule recipe with sha256-verified retrieval (planned).

## 8. Validation And Managed Artifacts

- **Local modifications to `validate.sh`:** **None.** `sha256sum validate.sh = 0a9878c8…` matches `meta/validate.sh` byte-for-byte at the pinned Science SHA. The inventory's `structural_diff: []` is correct. — *recurring candidate convention*: this project is a clean reference for how the MAV plan's "validator as managed artifact" model would land.
- **Project-specific environment assumptions:** `validate.sh` sources `.env` for `SCIENCE_TOOL_PATH`. The project's `.env` (~56 bytes) is not committed; the symlink `science -> /mnt/ssd/Dropbox/science` exists at the repo root partly to make `science-tool` resolvable from worktrees. These are project-specific glue, not validator drift.
- **Checks that likely belong upstream:** All 17 sections of `validate.sh` are upstream-canonical (matched against `meta/validate.sh`). — `mav-input` (clean evidence: this project is an exemplar of validator-as-managed-artifact working as intended).
- **Checks that are correctly project-specific:** None observed — `validate.sh` is unmodified. Project-specific validation lives elsewhere: `code/scripts/validate_select_run.sh`, `preflight_select_env.R`, the test suite (`code/scripts/tests/test_select_toy_dag.py`, `test_snakefile_select_wiring.py`).
- **Drift solvable by managed artifact versioning:** The project itself shows no validator drift — but the *registered local entity kinds* (`meta`, `pre-registration`, `synthesis`, `guide`, `modality-guide`, `curation-sweep`) point at a related drift surface that MAV would not solve: the curation sweep documents (line 33) that "the project relies on several document kinds that the current profile/tooling does not register cleanly … this now produces persistent health noise". `mav-input` does not apply here — this is a profile/schema gap, not a validator-text gap.
- **Drift NOT solvable by MAV (missing profile / schema / entity model):**
  - The pipeline-axis gap (`profile: research` flattening a research-plus-pipeline project) — *missing profile*.
  - The local entity kinds registered in `knowledge/sources/local/manifest.yaml` — *missing schema upstream*; the project has worked around it correctly via the `typed-extension` strictness setting, but the workaround is itself the evidence.
  - Reading-state for papers (`status: read|unread|abstract-read|summarized`) being co-located with work-status on the same `status` axis — *missing schema*; would need a separate `reading_state` field or paper-specific status enum.

## 9. Project-Grown Conventions

- **Modality guide system** — `doc/guides/modalities/{panel-mutation-data,wes-mutation-data,cross-study-aggregation,driver-detection,variant-annotation}.md` + `doc/guides/audit-process.md` that orchestrates them — **useful for ≥2 projects** (audit-process.md explicitly cites mm30 as origin) — *worth a future focused design pass*. Frontmatter uses project-local `type: modality-guide`.
- **Pre-registration as first-class entity** — `doc/meta/pre-registration-tNNN-…md`, registered as a project-local kind, body includes locked decision rules + thresholds — **likely useful for ≥2 projects** (the `2026-04-25-hypothesis-phase` plan touches a related axis upstream) — *recurring candidate convention*.
- **Curation sweep ledger** — `doc/meta/curation/curation-sweep-YYYY-MM-DD.md` with `generated_at` / `source_commit` / `mode: propose` / `applied_changes: 0` / `pending_decisions: 4` — strong machine-readable shape — *worth a future focused design pass*.
- **Discussion entity with `focus_type` + `focus_ref`** — `doc/discussions/YYYY-MM-DD-<slug>.md` — *project-grown*; could be useful upstream as a "decision-in-progress" record.
- **Pipeline-review entity** — `type: review`, with `plan: <path>` and `overall: WARN|PASS|FAIL` — *project-grown*; mirrors the existing `science:review-pipeline` skill shape.
- **Verdict polarity tokens + backfill audit** — `[+] / [~] / [?] / [-]` polarity tokens inside interpretation prose, plus the dedicated TSV at `doc/plans/2026-04-19-verdict-token-backfill-audit.tsv` that lifts them to a structured table — *project-specific, but the underlying need (typed verdict per interpretation) is recurring*.
- **Data-access gate record** — `doc/plans/t111-data-gate-record.md`, an explicit record of access-status decisions per data source (URL / SHA256 / DAC requirement / outcome) — *project-grown but reusable for any project with controlled-access data*.
- **Run-tagged interpretation frontmatter** (`mode`, `input`, `workflow_run`, `prior_interpretations`) — *project-grown*; possibly useful upstream for any pipeline-driven research project.
- **Hermetic notebook env** — `code/notebooks/{pyproject.toml,uv.lock}` distinct from project root — *project-specific specialization*.
- **Reading-progress as paper-status** — `status: unread|abstract-read|read|summarized` — *project-specific (and arguably overloads `status`)*.
- **Layered task `group:` taxonomy** — `audit-fixes / audits / meta / meta-analysis / pipeline / questions / searches` — *useful for ≥2 projects*; cross-project comparison candidate.
- **Project-local entity-kind manifest** (`knowledge/sources/local/manifest.yaml` with `strictness: typed-extension`) — *project-specific implementation of an upstream gap*; the design pattern (allow project-local kinds without breaking validators) is broadly useful.

## 10. Candidate Upstream Changes

| Candidate | Evidence | Scope | Priority | Migration Cost | Notes |
| --- | --- | --- | --- | --- | --- |
| Multi-axis profile (research + pipeline / research + software) | `science.yaml.profile: research` + `aspects: [computational-analysis, software-development]`; AGENTS.md "Active Roots" lists `code/workflows/`, `code/scripts/`, `code/config/` first; doc/code tracked-file counts essentially tied (147 / 140) | recurring | medium | medium | Hypothesis until peer projects confirmed. The `aspects` list is the current, incomplete workaround. |
| Project-local entity kinds (`meta`, `pre-registration`, `synthesis`, `guide`, `modality-guide`, `curation-sweep`) as a first-class upstream surface | `knowledge/sources/local/manifest.yaml`; `doc/meta/curation/curation-sweep-2026-04-22.md` line 33 explicitly flags the persistent health-noise these create | recurring | high | medium | The workaround works (`strictness: typed-extension`), but several of these kinds (pre-registration, synthesis, guide, curation-sweep) appear in peer projects too. |
| Pre-registration as first-class entity with locked-thresholds shape | `doc/meta/pre-registration-t077-glmm-logit-pooling.md` (365 lines), `doc/meta/pre-registration-t126-sbs1-lrr-bias-test.md`; the upstream `2026-04-25-hypothesis-phase` plan touches a related axis | recurring | high | low | Path `doc/meta/pre-registration-*` already matches the audit-plan expectation. |
| Reading-progress as a separate axis from work-status (`reading_state` enum) | inventory `status` enum mixes `unread/abstract-read/read/summarized` (paper reading-state) with `active/proposed/in-progress/deferred/draft` (work-status); 58 paper files use the reading-state values | recurring | medium | low | Paper-specific or generic-document `reading_state`; cleaner than overloading `status`. |
| Modality-guide system + audit-process orchestration | `doc/guides/modalities/{panel,wes,cross-study,driver,variant-annotation}-…md`; `doc/guides/audit-process.md` cites mm30 as origin; one bias audit (`doc/meta/bias-audit-cross-study-aggregation-pipeline.md`) | recurring | medium | medium | Audit-process explicitly says "adapted from MM30" — strong cross-project signal. |
| Dataset descriptor with SHA256 + DOI + assembly + retrieved-date table | `doc/datasets/replication-timing-constitutive-regions.md`, `doc/datasets/normal-tissue-spectra.md`; `doc/plans/t111-data-gate-record.md` extends the same idea to access-controlled sources | recurring | medium | medium | Could be lifted to a structured frontmatter sequence rather than a markdown table. |
| Datapackage manifests next to `results/<run>/` directories | `t128` task ("Emit retroactive datapackage.json manifests for results/poc-2026-04-17/ and results/signature-brca-2026-04-22/") explicitly recognizes this gap; `datapackages: []` in inventory | recurring | medium | medium | `mav-input` adjacent: the manifest is project content, but a templated emitter could be a managed artifact. |
| Interpretation entity with `mode` / `input` / `workflow_run` / `prior_interpretations` fields | `doc/interpretations/2026-04-22-t122-rt-brca-pilot.md` and 7 peers; the `## Verdict / ## Run Surface / ## Main Result` body shape is consistent | recurring | medium | low-to-medium | The `input` string would benefit from being structured (`{matched, unmatched, cancer_type}`). |
| Verdict polarity tokens (`[+]/[~]/[?]/[-]`) lifted to a typed entity | `doc/plans/2026-04-19-verdict-token-backfill-audit.tsv` is itself the backfill artifact | recurring | low | medium | Hypothesis-strength claim until cross-project usage confirmed. Currently inline tokens, post-hoc table. |
| Curation-sweep ledger as first-class entity | `doc/meta/curation/curation-sweep-2026-04-22.md` with structured frontmatter (`generated_at`, `source_commit`, `mode`, `applied_changes`, `pending_decisions`) | recurring | medium | low | Already shaped as machine-readable; project registers it locally. |
| Discussion entity with `focus_type` + `focus_ref` fields | `doc/discussions/2026-04-24-t124-q009-fork-decision.md` (and two peers) | project-specific | low | low | May or may not appear in peer projects. |
| Project-grown convention: hypothesis lifted out of `specs/research-question.md` to a typed family | `specs/research-question.md` lines 39-42 carry one inline hypothesis | upstream | low | low | Already a known upstream direction; this is just confirming evidence. |
| Code/notebook → task linkage (sidecar or comment-block) | 3 marimo notebooks named `tNNN_…py`; commit subjects use `fix(tNNN): …`; no programmatic linkage from script → task | recurring | low | low | Lightweight: a `# task: tNNN` header comment would suffice. |
| Stale-frontmatter outliers as health-check evidence | `doc/interpretations/2026-04-18-t070-poc-comparison.md` (no frontmatter, called out in curation sweep), `doc/plans/t111-data-gate-record.md` (no frontmatter), `doc/meta/handoff-2026-04-25-t131-dndscv.md` (no frontmatter), `doc/audits/2026-04-13-create-combined-gene-cancer-freq-table-audit.md` (no frontmatter) | project-specific | low | low | Drift instances; project-internal cleanup, not upstream change. |
| Inventory v0 bug: `entity_id_prefix_counts: {"<unparseable>": 124}` contradicts the immediately-following `entity_id_paths` map of 124 fully-parsed CURIEs | `inventory/cbioportal.json` lines 367-368 vs lines 370-743 | upstream-tooling | low | low | Phase-1 script bug — flag for `tooling-notes.md`. |
| Inventory v0 gap: gitignored top-level dirs (`archive/`) not surfaced | `archive/` is in `.gitignore` and contains `data_pkg_todo.md`, `eda/`, `extra/`, `genie.rgz`, etc.; AGENTS.md documents it but inventory misses it | upstream-tooling | low | low | Suggest a `gitignored_top_level_dirs` summary in inventory v1. |
