# Downstream Project Audit: natural-systems

## Header

- **Project root:** `/mnt/ssd/Dropbox/natural-systems`
- **Project SHA at audit:** `e3c978e0c67af98a81da997c389ba34041595ffc` (inventory-pinned; project HEAD has since moved to `01a7a98660502134c9652f4f323341c5d847dba2`, +1 commit `feat(h06): cli + loaders for morphism-edge validator (t328)`)
- **Dirty tree summary:** clean at inventory time (`status_porcelain: []`)
- **Inventory artifact:** `docs/audits/downstream-project-conventions/inventory/natural-systems.json`
- **Science SHA used for comparators:** `a059f7d3dd46fb197df14339e6e9576bbb5615dd`
- **Auditor / agent:** Claude Opus 4.7 (Phase 2 audit subagent)
- **Audit date:** 2026-04-25

## Known Starting Conditions

- `doc/meta/entity-health/unresolved-ref-review-2026-04-23.md` — review of remaining `science-tool health` unresolved refs. The plan's "~50 unresolved references" note is a high-water mark; per this internal review the count is now `19` (14 semantic-triage `topic:*` labels, 5 stale plan/report ids). Not classified as drift.
- `validate.sh` — managed-artifact migration target; per plan, local mods are recorded as MAV-input evidence, not drift.
- `archive/` (git-ignored) holds an earlier generation of plans / source files; expected scaffolding, not active drift.

---

## 1. Project Center Of Gravity

- **Primary shape:** hybrid (software app + research project). The Vite + React + TypeScript guide app is the deliverable; everything else (catalog, registries, lenses, knowledge graph) is research output that feeds it.
- **Dominant tracked directories:**
  - `src/` (1135 files) — application code, simulations, lens/model/parameter registries
  - `content/` (2049 files, 40 markdown) — authored YAML model/formula/visual records and long-form prose chapters
  - `doc/` (576 markdown) — Science-managed research documentation
  - `public/` (523) and `research/` (501) — generated `datapackage.json` mirrors (117 datapackages)
  - `scripts/` (353), `workflows/` (342), `specs/` (40), `knowledge/` (43)
- **Dominant untracked / symlinked directories:** `node_modules/` (405 entries), `.venv/`, `.worktrees/` (empty), no symlinks reported. Filesystem also carries `archive/` and `dist/` (gitignored).
- **`science.yaml` profile / aspects:** `profile: software`; `aspects: [computational-analysis, hypothesis-testing, software-development]`; `ontologies: [astronomy, biology, chemistry, earth, information, math, physics, units]`; `knowledge_profiles: {local: project_specific, curated: []}`; `data_sources: []`.
- **Does profile + aspects capture the real project?** partial — `software` is honest about the app, and the three aspects cover the research surface, but the project actually runs three centers of gravity in parallel: a content-authoring system (long-form chapter prose + YAML registries), a research project (hypotheses, questions, plans, interpretations), and an app. The `software` profile silently omits the chapter-prose / YAML-registry layer where the bulk of authored content lives.
- **Multi-axis needed?** yes — `aspects` already encodes "we are also a research project," but the catalog/content authoring axis (`content/prose/chapter/*`, `content/models/*.yml`, `content/visuals/`) is unmodeled. A `content-authoring` aspect or analogous axis is the natural place for it.

## 2. File And Directory Organization

- **`doc/` taxonomy and naming:** *recurring candidate convention* for the standard families (`background/`, `discussions/`, `interpretations/`, `papers/`, `plans/`, `questions/`, `reports/`, `searches/`, `topics/`, `meta/`). Plus *project-specific* additions (`figures/`, `guides/`, `inquiries/`, `refactors/`). Also four sibling refactor-plan pair directories at the top of `doc/` — `guide-data-model-refresh/`, `meta-model-refinement/`, `project-organization-migration/`, `provenance-derivation-redesign/` — each containing exactly `desired_file_structure.md` + `files_to_remove.md`. This is a *project-grown convention* keyed off the user's global "refactoring documentation" rule (`/home/keith/.claude/CLAUDE.md`).
- **`docs/` vs `doc/`:** *accidental drift / parallel hierarchy*. `docs/plans/` (8 files) and `docs/superpowers/{plans,specs}/` (44 files total) sit alongside `doc/plans/` (197) with no clear separation rule visible in `AGENTS.md` or `CLAUDE.md`. Files in `docs/` typically lack frontmatter (e.g. `docs/plans/2026-04-14-appendix-a-data-model-refresh.md` opens with a markdown H1, not YAML). `docs/superpowers/` looks like a holdover from when the superpowers skills wrote there directly. Inventory captures this asymmetry: `doc/` has 553 markdown files, `docs/` has 44.
- **`specs/` usage:** *valid project-specific convention* with two patterns. (a) Date-prefixed design docs (`specs/2026-04-22-morphism-generator-families-design.md`, 38 files), each with `id: "spec:..."`, `type: "spec"`, `related_questions/_specs/_interpretations/_tasks` references. (b) Non-dated concept specs (`specs/research-question.md`, `specs/scope-boundaries.md`, `specs/catalog-canonical-core.md`). (c) `specs/hypotheses/h01...h07.md` — 7 hypotheses live here rather than in `doc/hypotheses/`. Mixing date-prefixed implementation specs with concept specs and hypotheses in one directory is *unclear convention*: outsiders cannot tell from path which is which.
- **`tasks/` layout and archival:** *valid project-specific convention* with one *accidental drift* signal. `tasks/active.md` (2570 lines, 232 task entries, 114 of which carry `status: done`); `tasks/done/2026-03.md` and `tasks/done/2026-04.md`. Archival happens by month, but completed tasks are not promptly moved out: 49% of `active.md` entries are already `done` at the inventory snapshot.
- **`knowledge/` (generated vs source):** mixed — `knowledge/sources/{local,project_specific}/` are canonical authored YAML; `knowledge/graph.trig` is generated; `knowledge/scripts/` (4 files) and `knowledge/layers/*.trig` are explicitly described as **retired** in `CLAUDE.md` ("Do not use the retired `knowledge/scripts/` layer builders or `knowledge/layers/*.trig`."). These are *stale scaffolding* — the project explicitly migrated away but did not delete.
- **Code locations (`src/` / `code/` / `scripts/` / `workflow(s)/` / notebooks / viewer apps):** *recurring candidate convention* — `src/` is the app (35 second-level subdirs, dominated by `models/`, `chapters/`, `simulation/`, `natural/`, `lenses/`, `formulas/`, `guide/`). `scripts/` (353) holds analysis + authoring CLI (`scripts/cli/{commands,format,query,writers}/`) + an unusually heavy `scripts/health/` (105 files) and `scripts/audit/` (11). `workflows/` (342) holds Snakemake pipelines for nine analysis families (`claim-extraction/`, `dimensionless-analysis/`, `domain-analysis/`, `math-form-analysis/`, `openalex-analysis/`, `primitive-analysis/`, `theme-validation/`, `catalog-analysis/`, `common/`). No notebooks. Viewer is just `src/`.
- **Data / results / log / model locations:** generated `datapackage.json` files appear under both `research/packages/<lens>/<id>/` (canonical) and `public/research/<lens>/<id>/` (web-served mirror) — 117 packages in each tree, byte-identical when run timestamps are stripped. `pipeline/graph-analysis/{data,results}/` and `pipeline/h03/`, `pipeline/dimensionless/` are gitignored. `papers/pdfs/` is gitignored except for `.gitkeep`.
- **Archive and migration directories:** `archive/` is gitignored top-level; per-feature migration plan pairs live under `doc/<topic>/`. Three "recon" plans live under `doc/plans/recon/` (`detail-page-parity-{behavior,dimgroup,facet,symmetry,variable}.md`) — each a parent-child plan pair. *Project-grown convention.*

## 3. Entity Model And Metadata

Sampling floor: `min(5, all)` per family + long-form (>300 line) and embedded-metadata files. The inventory flags 204 long-form and 154 embedded-metadata files. I sampled representatives from each family present.

- **Hypotheses:** 7 (all sampled — floor met). `specs/hypotheses/h01..h07.md`. Frontmatter is consistent: `id: hypothesis:hNN-slug`, `type: hypothesis`, `title`, `status`, `priority`, `related: [...]`, `created`, `updated`. Status uses {`conjecture`, `proposed`, `partially supported`}; H05 alone uses `partially supported` (with space) where Science would conventionally use a hyphen or underscore. Only h07 carries the new `phase: candidate` (`phase: candidate` from `2026-04-25-hypothesis-phase.md` plan).
- **Propositions / claims:** N/A — see inventory §markdown.observed_values. No proposition or claim entities. The closest is `evidence` work captured in `doc/discussions/`/`doc/interpretations/` and structured "claims" inside the `workflows/claim-extraction/` pipeline (76 files), which are workflow inputs, not first-class entities.
- **Questions:** 94 in `doc/questions/qNN-slug.md`. Sampled q01, q06 (note: id is `question:q06-...`, no naked `Q01`), q19 (retired), q22, q61. Frontmatter shape consistent: `id`, `type: question`, `title`, `status`, `tags`, `ontology_terms`, `datasets: []`, `source_refs`, `related`, `created/updated`, sometimes `priority`. Statuses: `partially-answered`, `open`, `retired` (with `retired_reason`), `answered`, `partially-resolved`. **Duplicate id:** the inventory flags `question:q61-variational-as-annotation` appearing both at `doc/questions/q61-variational-as-annotation.md` and `doc/topics/variational-unity.md` — the topic file copies q61's id while serving as a topic; this is *accidental drift* (a topic should not carry a `question:` id).
- **Papers:** 14 in `doc/papers/`. Sampled `Brunton2022.md`, `Legatiuk2021.md`, `Myers2025.md`, `categorical-composition-trio-2023-2025.md`, `MathModDB-review.md`, `survey-attractor-topology-methods.md`. Frontmatter: `id: paper:<key>`, `type: paper`, `title`, `status`, `tags`, `ontology_terms`, `datasets`, `source_refs`, `related`, dates. BibTeX keys `[@AuthorYear]` flow into `papers/references.bib` (293 entries). The two "survey-*" and "categorical-composition-trio-*" files are *project-grown literature-survey* entities — paper-typed but not single-paper summaries. Long-form files: `Brunton2022.md` (562), `survey-lie-symmetry-classification.md` (529), `survey-attractor-topology-methods.md` (595).
- **Topics / background:** *unclear convention*. Two parallel topic locations:
  - `doc/topics/` (14 files) — typed `concept` (id prefix `concept:` for most; one is the duplicated `question:q61-...`)
  - `doc/background/topics/` (1 file: `model-classification-and-inter-model-relationships.md`) — typed `topic` (id prefix `topic:`)
  Inventory's `entity_id_prefix_counts` confirms `concept` (5) and `topic` (4); these refer to the same conceptual layer. Stale or dual convention.
- **Datasets:** N/A — see inventory §science_yaml. `science.yaml` `data_sources: []`. There is no `doc/datasets/` family. Datasets-as-frontmatter-field appears 52 times in `doc/` (mostly `datasets: []`) and serves only as a reference field. The actual catalog/registry data lives as YAML under `content/models/*.yml` and `src/natural/registry/*` — not modeled as Science datasets.
- **Interpretations:** 71. Sampled `2026-04-25-parameter-derivation-dag-v2.md` (753 lines, embedded-metadata-flagged), `2026-04-13-parameter-derivation-dag.md` (491), `2026-04-11-h03-h04-topology-dynamics-synthesis.md` (319), `2026-03-29-lens-refinement-and-temporal-gap.md` (304), `2026-03-22-t051-t052-t053-symmetry-and-dynamics-surveys.md` (327). Frontmatter: `id: interpretation:DATE-slug`, `type: interpretation`, `title`, `status`, `related`, `created/updated`. Long-form interpretations frequently embed `<!-- ... -->` section markers and tabular metadata; the >300-line files commonly hold full "Verdict / Methodology / Results / Caveats" essays.
- **Reports / syntheses:** 87 in `doc/reports/`. Sampled `2026-03-31-lens-validation-summary.md`, `2026-04-22-extends-generator-pilot-audit.md`, `2026-04-22-provenance-derivation-rerun.md`, `2026-04-23-q93-second-midpoint-object-pilot.md`, `2026-04-23-formula-artifact-producer-audit.md`, `2026-03-26-lens-model-mentions-audit.md`. Frontmatter is *uneven*: 26 files use `id: "doc:DATE-slug"` while only 4 use `id: "report:DATE-slug"`. Type values: `report` (29), `validation-report` (1), bare `report` (1). One report (`2026-03-26-lens-model-mentions-audit.md`) has *no frontmatter at all* (auto-generated by a script). *Inconsistent id prefix.*
- **Plans / pre-registrations:** 191 plans in `doc/plans/`. Pre-registrations live in `doc/meta/pre-registration-*.md` (4 files: `pre-registration-q54...`, `pre-registration-t085-t086.md`, `pre-registration-t092.md`, `pre-registration-t214.md`) and are typed `plan` with `id: plan:pre-registration-...`. Plan id-prefix usage: 50 `plan:`, but inventory also surfaces 7 `vis:*` ids (entity-type definitions inside `doc/plans/2026-04-18-visual-registry-foundation.md`, declaring future `vis:*` entities — fine) and 2 `interpretation:*` (test fixtures in `doc/plans/2026-04-08-claim-evidence-population-plan.md`, dated `2026-01-01-test-*`; these look like test ids that snuck in — accidental drift). Some plans live at `docs/superpowers/plans/` (no Science frontmatter) and `docs/plans/` (no frontmatter). Five "umbrella" non-dated plans: `kg-project-migration-guide.md`, `meta-model-specification.md`, `diversity-improvement-plan.md`, plus the `recon/` parent-child plans (5 files).
- **Tasks:** Single file `tasks/active.md` (2570 lines, 232 entries) + `tasks/done/2026-03.md`, `tasks/done/2026-04.md` (combined 1082 lines). Format: `## [tNNN] Title`, then a kebab-list of `priority`, `status`, `aspects`, `related`, `blocked-by`, `created`, `completed`. IDs are bare `tNNN` in the heading but referenced as `task:tNNN` from frontmatter elsewhere. Status enum (in `active.md`): `proposed` (115), `done` (114), `deferred` (2), `blocked` (1). Tasks are **not promptly archived** — 114/232 entries in `active.md` are already done. Also: `t001..t003` `Result:` paragraphs are inline in active.md, suggesting task entries grow into result narratives that should be linked, not embedded.
- **Project-specific entities:** the most distinctive — 234 `content/models/*.yml` model records, 281 `content/visuals/{model,concept,dimgroup,interp,limit,math,model,primitive,sktran}/*.yml`, plus `content/{formulas,parameters,primitives,behaviors,facets,limits,symmetries,variables,dimgroups,lens-sections,lens-topics,skeleton-transformations,interpretations}/*.yml`, plus `content/appendices/*.yml` and `content/prose/chapter/*.md`. The chapter prose files (`content/prose/chapter/01-fluid-dynamics.md` ... `20-control-information-theory.md`) hold per-section embedded structured metadata via `<!-- model: <id> -->` HTML comments + `**Key:** Value` lines + classification tables. Inventory's embedded-metadata heuristic flags this correctly — these are *project-grown structured-prose entities*. `specs/hypotheses/`, `specs/catalog-canonical-core.md` (a curated "tier 1 / tier 2" model list), and the `nature` authoring CLI (`scripts/cli/`) all reference these.

Cross-cutting observations:

- **Missing entity families (clearly present in prose or paths):** none of the standard families is missing. The catalog model / formula / visual / lens / parameter / primitive families exist as YAML registries under `content/` and `src/natural/registry/`, not as Science doc-frontmatter — that's a deliberate split, not a missing family. *Specialization, not drift.*
- **Duplicated information across entity types:** `doc/topics/variational-unity.md` reuses `id: question:q61-...`. Hypothesis/spec/plan triads frequently overlap (e.g. `specs/hypotheses/h05-...` ↔ `doc/plans/2026-04-25-h05-phase-e-methodology.md` ↔ `specs/2026-04-13-data-fitting-pilot-design.md` ↔ `doc/inquiries/data-fitting-pilot-review.md`). The four refactor-pair directories under `doc/` (`*-refresh/`, `*-refinement/`, `*-migration/`, `*-redesign/`) duplicate content that elsewhere would live in `doc/plans/`.
- **Inconsistent `id` prefixes:** `doc/reports/*.md` uses `doc:DATE-slug` (26) far more often than `report:DATE-slug` (4). `doc/meta/next-steps-*.md` uses `plan:next-steps-DATE` for older files but `doc:next-steps-2026-04-24` for the latest — *recent drift*. `doc/inquiries/data-fitting-pilot-review.md` is typed `report` with id `report:data-fitting-pilot-review` even though it lives in `inquiries/`.
- **Overloaded `type` / `status` / `phase` values:** `status` enum sprawl is large — 25 distinct values across the project (inventory §observed_values.status). Variants that look like duplicates: `complete` (76) vs `completed` (1); `partially-answered` (13) vs `partially supported` (1) vs `partially-resolved` (1); `agreed` (1, hypotheses); `current` (1); `final` (1); `sketch` (1, inquiry); `closed` (1); `published` (2). `type: validation-report` (1) is a one-off variant of `report`.
- **Fields that should likely be enums:** `status`, `priority` (`P1/P2/P3` — already enum-like but not validated), `mode` (`standard`, etc., 54 uses), `focus_type` (49 uses, values include `approach`), `inquiry_type` (`causal`, 1 use), `question_type` (45 uses).
- **Fields that should likely be structured objects, not strings:** `source_refs` mixes paper ids and inline strings (`source_refs: - Schembera2024 - paper:Legatiuk2021`); `related` mixes typed entity ids (`question:q05`) with bare strings (`question:q45` — no slug) and full file paths (`doc/discussions/...md`) (e.g. `specs/2026-04-22-morphism-generator-families-design.md`). Numbered question references like `question:q45` and `question:q46` (no slug) appear in hypothesis frontmatter; the unresolved-ref review confirms these are stale.
- **Inline information that should likely be metadata:** task `Result:` paragraphs in `active.md` and `done/`. Long-form chapter prose embedded model classification tables (`content/prose/chapter/0X-*.md`).
- **Embedded entity metadata blocks inside long prose / plans:** 154 files flagged. The chapter prose files (chapters 01–20 + appendices + section-intros, 26 long-form chapters) carry per-model `<!-- model: id -->` blocks plus `| Classification | Value |` tables and `**Key:** value` lines — this is the project's deliberate prose-as-registry pattern. The "false positives" set includes long plans where lists of `key: value` lines appear in a discussion of fields, e.g. `doc/plans/2026-03-07-guide-structural-refinements.md` (13 candidate starts). Per `tooling-notes.md` the heuristic is purely lexical; flag this for the synthesis pass but do not chase per-flag.
- **Inconsistent use of `related` / `datasets` / `source_refs` / `datapackage` / `local_path` / `consumed_by` / `produces`:** `related` is the dominant cross-entity link field (326 uses in `doc/`, 11 in `specs/`); `source_refs` (139); `datasets` (52, mostly `[]`); `consumed_by`/`produces`/`datapackage`/`local_path`: not used. `source` appears once (`doc/papers/MathModDB-review.md`). Specs use a richer related-family split: `related_questions`, `related_specs`, `related_interpretations`, `related_reports`, `related_tasks` — *valid project-specific convention* worth surfacing for synthesis.
- **External accession / ontology usage:** arXiv ids (1 frontmatter `arxiv:` field; many in prose, e.g. `arXiv:2108.06288` in `tasks/active.md`). DOIs in BibTeX. No PubMed/PMCID usage observed (project domain is mostly math/physics, not biomedical). HGNC/UniProt/GO/UBERON/OncoTree/PDB/Pfam/InterPro/GEO: not applicable. Internal ontologies: project declares `ontologies: [astronomy, biology, chemistry, earth, information, math, physics, units]` in `science.yaml` and uses `ontology_terms: [...]` lists per question/paper. The `ontology_terms:` field appears 54 times. Hypothesis: this functions as a project-local controlled vocabulary parallel to external ontologies. Marked unclear — synthesis pass.

## 4. Planning, Pre-Registration, And Decision Records

- **Implementation plans:** `doc/plans/*.md` (191) is the canonical location with `id: plan:DATE-slug`, `type: plan`, `status` (e.g. `draft`, `agreed`, `complete`, `superseded`). Often paired: `<topic>-design.md` + `<topic>-implementation.md`. Plus 8 `docs/plans/*.md` *without frontmatter* — these look like they bypass the convention. Plus 36 `docs/superpowers/{plans,specs}/*.md` — earlier-generation plans authored by superpowers skills, no frontmatter.
- **Design / spec docs:** `specs/*-design.md` (38, all date-prefixed) carry `id: spec:DATE-slug`, type `spec`, rich `related_*` link family. `specs/{research-question,scope-boundaries,catalog-canonical-core}.md` are non-dated permanent specs. `specs/hypotheses/h0X-*.md` are hypotheses, not specs — placement is *unclear convention*.
- **Pre-registrations:** `doc/meta/pre-registration-{q54,t085-t086,t092,t214}.md` (4 files), typed `plan`, prefixed `plan:pre-registration-...`. They are *not* in `doc/pre-registrations/`; the location is `doc/meta/`. Per the plan's note ("downstream projects may use `doc/pre-registrations/`, `doc/meta/pre-registration-*`, or inline task notes") this is the second variant.
- **Next-step / gap-analysis docs:** 11 in `doc/meta/next-steps-DATE.md`, all typed `meta` or `plan`. Recurring weekly cadence (2026-03-11, -18, -29, -04-03/04, -11/12, -14, -20, -24). Latest one (`2026-04-24`) drifts in id prefix from `plan:` to `doc:`.
- **Curation sweeps:** `doc/meta/skill-feedback.md` (438 lines) reads as a curation/sweep journal. `doc/reports/*-audit.md` (multiple) and `doc/meta/dashboard-data-gaps.md`, `doc/meta/entity-health/unresolved-ref-review-2026-04-23.md`, `doc/meta/pending-compositions.md` together form an entity-health curation surface.
- **Handoffs:** `doc/plans/2026-04-25-h05-discriminating-test-handoff.md` is one explicit handoff; `doc/plans/recon/*.md` are pre-implementation handoff plans for detail-page parity migrations.
- **Audit reports:** 87 in `doc/reports/` plus `doc/meta/entity-health/`. Mix of `*-audit.md`, `*-summary.md`, `*-calibration.md`, `*-rerun.md`. Type-prefix drift to `doc:` is most pronounced here.
- **Project roadmap / research plan files:** `RESEARCH_PLAN.md` at root (32 lines, no frontmatter, the standard Science template). `specs/research-question.md` (decomposes into Q1–Q5). `specs/scope-boundaries.md`. `specs/catalog-canonical-core.md` (a curated "must-have models" tier list).

## 5. Task Lifecycle

- **`tasks/active.md` shape:** *mostly recurring convention, with archival drift*. Single flat file, `## [tNNN] Title` headings, kebab-list metadata. Heavy: 2570 lines, 232 entries.
- **Done archive pattern:** `tasks/done/YYYY-MM.md` — only 2 monthly archives exist (2026-03, 2026-04). Same shape as active.
- **Completed tasks remaining in active files:** **yes — 114/232 entries (49%)** in `active.md` carry `status: done`. These should be moved to `tasks/done/2026-04.md` (or a new month). *Accidental drift / archival lag.*
- **Task ID format:** `tNNN` lower-case in headings; `task:tNNN` in frontmatter `related` lists. Consistent.
- **Status values observed (active.md):** `proposed` (115), `done` (114), `deferred` (2), `blocked` (1). Note: `in-progress` is in the global enum (5 uses across project) but absent from active.md — completed work jumps directly from `proposed` to `done`.
- **Use of `priority` / `aspects` / `group` / `related` / `blocked-by` / `created` / `completed`:** `priority` (P1/P2/P3) consistent. `aspects` populated, e.g. `[computational-analysis]`, `[software-development]`. `group` not used in tasks. `related` present and points to question/hypothesis/discussion ids. `blocked-by` used (e.g. t010 blocked by t009). `created`/`completed` ISO dates.
- **Code, data, reports, interpretations link back to task IDs:** *partial*. Reports and interpretations reliably cite tasks (e.g. `report:data-fitting-pilot-review` related to `task:t241`); search files use `task_ref: task:t021`. Code does *not* link back — `src/` and `scripts/` are not annotated with task ids.
- **Are task groups a stable concept worth modeling:** weakly. Tasks are de-facto grouped by hypothesis (h0X) or workstream (q93) via `related`, but no first-class `group` field exists. Hypothesis-keyed task swarms (e.g. h05 phase E, h06 morphism inventory) are the dominant grouping pattern; hypotheses already serve this role.
- **Do tasks contain enough provenance, or should they link to result entities:** *should link to result entities.* Many done tasks include `**Result:** ...` paragraphs inline (t001, t002, t003, etc. — see lines 9, 19, 29 of `active.md`). These are essentially short interpretations crammed into the task entry. The project already has `doc/interpretations/` and `doc/reports/`; tasks should link out instead.

## 6. Code, Workflows, And Tests

- **Code root convention:** `src/` for app code (TS/React + simulation engines + lens/registry/formula systems). `scripts/` (TS + Python) for build-time and analysis tooling. *recurring candidate convention*.
- **Snakemake layout (if any):** `workflows/<analysis-family>/` with `Snakefile`, `config.yaml`, plus `manifests/`, `results/`, `applied/`, `review-queue/` (varies). Nine analysis families. *Project-grown but recurring within project.*
- **Script naming and grouping:** kebab-case TS scripts at `scripts/*.ts`, plus a deeper `scripts/cli/{commands,format,query,writers}` system implementing the project's `nature` authoring CLI. `scripts/health/` (105 files) is a substantial in-tree health subsystem with `checkers/`, `reporter.ts`, `runner.ts`, `scorer.ts`, `bundle.ts`, `index.ts`. `scripts/audit/` is a parallel audit subsystem. *Project-grown convention worth synthesis.*
- **Notebook placement:** none. `code_layout.notebooks.count = 0`.
- **Tests colocated vs root:** *both*, intentional. Vitest unit tests colocated under `src/<x>/__tests__/*.test.ts` and `scripts/**/__tests__/*.test.ts`. Playwright integration tests at `tests/ui/`. Three router integration tests at `src/__tests__/`.
- **Generated model / schema code:** `src/chapters/generated/*.json` is gitignored except for an explicit allow-list (`guide-data.json`, `guide-content.json`, `prose-content.json`, `prose-health.json`, `visual-registry.json`, `chapter-prose-registry.json`). `src/natural/schema/` holds canonical zod/TS schemas (and is referenced as the "authority" by `specs/2026-04-11-canonical-spec-authority-design.md`). *Project-grown recurring*.
- **Viewer / app subprojects:** the entire `src/` is the viewer; no subproject division.
- **Config and environment files:** `package.json`, `tsconfig.json`, `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`, `commitlint.config.mjs`, `pyproject.toml`, `uv.lock`, `.husky/`, `.scienceignore` (single line: `doc/sync/`), `.audit-ignore` not present.
- **Linkage from code artifacts to tasks / questions / hypotheses / datasets / plans:** *weak from code → entities.* Reverse direction is well-populated: tasks/plans/specs reference code paths in prose (`scripts/...`, `src/...`). Code files do not carry sidecar metadata or in-source provenance frontmatter. The `nature` authoring CLI does encode a content-authoring contract (`scripts/cli/writers/transactional.ts` etc.) but not a code-to-research-entity link.
- **Would code artifacts benefit from lightweight metadata blocks or sidecar manifests?** case-by-case. Pipeline producers (`scripts/build_primitive_packages.py`, `scripts/extract-dimensionless-package-data.py`, etc.) already feed structured `provenance` blocks into the 117 datapackages — so for *artifact-producing* scripts the sidecar already exists. For raw analysis scripts, a lightweight comment-block linking back to a task or question id would be enough.

## 7. Data, Results, And Provenance

- **Input data:** none externally configured (`science.yaml` `data_sources: []`). The project's "data" is the 234 model YAML records under `content/models/` — *tracked source metadata* authored in-tree.
- **Protected / manual data:** none. `protected_data_dirs: []` from inventory.
- **Public downloadable data:** *project-grown* via 117 `datapackage.json` records in `public/research/<lens>/<id>/` and the parallel `research/packages/<lens>/<id>/` tree. Each declares a `science-research-package` profile, lists CSV/JSON resources, and embeds a `research:` block with `target_route`, `provenance.{workflow, config, last_run, git_commit, inputs}`. Lenses covered: dimensionless-group (10), domain (19), equation-type (9), primitive (11), solution-behavior (10), theme (1). *Tracked small public data with explicit provenance.*
- **Intermediate outputs:** gitignored — `pipeline/graph-analysis/{data,results}/`, `pipeline/h03/`, `pipeline/dimensionless/`. Snakemake rules in `workflows/*/Snakefile` produce these.
- **Final results:** mixed. Markdown reports under `doc/reports/` are tracked. Generated JSON under `src/chapters/generated/` is gitignored except for a six-file allow-list (build outputs needed at runtime). *Tracked source metadata + ignored generated output with allow-listed published artifacts.*
- **Logs:** none tracked. `.gitignore` does not call out logs explicitly. `health-report.json` and `formula-investigation.md` under `doc/reports/` are explicitly ignored.
- **Models:** 234 YAML model records under `content/models/*.yml` are *tracked source metadata*. Trained ML models: none.
- **Descriptors / sidecars:** the per-package `datapackage.json` is the sidecar pattern, plus per-visual `content/visuals/<kind>/<id>.yml` records.
- **Datapackages:** 117 packages — see Public downloadable data. These follow the Frictionless data-package profile spec extended with a `research:` block.
- **Symlinked external storage:** none. Inventory `symlinks: []`.
- **`.gitignore` exceptions:** `src/chapters/generated/{guide-data,guide-content,prose-content,prose-health,visual-registry,chapter-prose-registry}.json` (six explicit re-includes after the bulk `src/chapters/generated/*` ignore). Also `!.claude/skills` re-include. *Recurring candidate convention* — ignore-then-pin pattern is clear and explicit.

- **Is source / provenance visible?** yes — both at the workflow level (`workflows/<x>/Snakefile + config.yaml`) and the artifact level (`datapackage.json` `research.provenance` blocks include `git_commit`, `last_run`, `inputs[]`).
- **Can publicly available data be reproduced through a pipeline?** yes — the nine `workflows/*/Snakefile` pipelines plus `npm run kg:export-sources` / `science-tool graph build` are the documented reproduction flow (see `CLAUDE.md` "Knowledge Graph" section).

## 8. Validation And Managed Artifacts

The inventory's `validate.structural_diff` is empty, but byte-level `diff` shows `validate.sh` is **not** structurally identical to canonical `meta/validate.sh`. This is a Phase-1 inventory limitation — see Reporting Back. Real local modifications below.

- **Local modifications to `validate.sh`:** non-trivial. Diff vs `meta/validate.sh` (Science SHA `a059f7d`):
  - Adds `.env` sourcing block (lines 12–19) for `SCIENCE_TOOL_PATH`
  - Renames the local-profile constant from `LOCAL_PROFILE="local"` to `LOCAL_PROFILE="project_specific"` and updates the `python -c` introspection block accordingly (lines 87, 115–117)
  - Replaces the `ontologies` list-shape check (3 lines) with a `knowledge_profiles.curated` missing/list-shape check (5 lines)
  - Restructures the `science-tool graph audit/validate/diff` invocation block: in the local copy, `SCIENCE_TOOL` is resolved at the top; in canonical, it is resolved inline at the audit site, with stricter error gating around `graph.trig` presence
  - The local copy treats unparseable graph-audit output as `warn` ("expected for fresh projects"); canonical treats it as `error`
  - Total: 80 lines of unified diff, ~14 distinct content blocks affected.
- **Project-specific environment assumptions:** sourcing `.env` for `SCIENCE_TOOL_PATH`. The project assumes a developer-installed `science-tool` reachable via either env-var or `resolve_science_tool` — and warns rather than errors when missing.
- **Checks that likely belong upstream `[mav-input]`:** the `.env` sourcing block (most projects need this), the `LOCAL_PROFILE` parameterization (canonical hard-codes `"local"`, but this project genuinely uses `project_specific` per its `science.yaml`). The `knowledge_profiles.curated` list-shape check looks like the right canonical replacement for the `ontologies` list-shape check (whether to keep both is a synthesis question).
- **Checks that are correctly project-specific:** none observed — the local mods all look like generic project-bootstrap concerns, not natural-systems-specific logic.
- **Drift solvable by managed artifact versioning `[mav-input]`:** all of the above. The `LOCAL_PROFILE` rename is the strongest evidence: the canonical validator hard-codes a value that should be configurable from `science.yaml`. MAV would let the project pin to a canonical version while flagging the legitimate local need for `LOCAL_PROFILE` parameterization upstream.
- **Drift NOT solvable by MAV (missing profile / schema / entity model):**
  - The 87 `doc/reports/` files using `id: doc:DATE-slug` instead of `id: report:DATE-slug` is a missing **report-id-prefix schema** — MAV cannot fix it because `validate.sh` does not check id-prefix conformance.
  - The 25-value `status` enum sprawl (with near-duplicates `complete/completed`, `partially-answered/partially-resolved/partially supported`) is a missing **per-type status enum** — needs an entity-model decision, not artifact versioning.
  - The dual `topic`/`concept` typing in `doc/topics/` vs `doc/background/topics/` needs **entity-model clarification**.
  - The `tasks/active.md` archival lag (114 done entries unmoved) is missing **lifecycle automation** rather than artifact drift.
  - The `docs/` vs `doc/` parallel hierarchy needs a **profile-level decision** about whether the project model should accept both, or which is canonical.

## 9. Project-Grown Conventions

- **Refactor-pair directories** — `doc/{guide-data-model-refresh,meta-model-refinement,project-organization-migration,provenance-derivation-redesign}/{desired_file_structure.md,files_to_remove.md}` plus `doc/refactors/<topic>/files_to_remove.md` — *project-specific, grown from a global user instruction* (see `~/.claude/CLAUDE.md`'s "Refactoring" section). Useful for ≥2 projects only if the upstream user instruction is the source; worth a focused design pass on whether Science wants a first-class refactor-doc family.
- **`content/<entity-family>/*.yml` registry layer** — 234 model YAMLs, 281 visual YAMLs, plus formulas, parameters, primitives, behaviors, facets, limits, symmetries, variables, dimgroups, lens-sections, lens-topics, skeleton-transformations, interpretations, appendices. *Project-specific*; this is the system Science calls "knowledge graph sources" but expressed as authored YAML, not Science frontmatter. Likely missing upstream support for "non-markdown entity types" — worth a design pass on whether Science should model arbitrary YAML/JSON entity registries alongside markdown frontmatter.
- **`science-research-package` datapackage profile** — 117 `datapackage.json` records under `public/research/` and `research/packages/` declaring `profile: science-research-package` with embedded `research: { target_route, provenance, cells, code_excerpts }` block. *Useful for ≥2 projects* (any project that publishes pipeline outputs alongside research routes).
- **`nature` authoring CLI** — `scripts/cli/{commands,format,query,writers}` with transactional writers, kind-derivation logic, prose/visual writer modules, plus colocated tests. *Project-specific* but an interesting specimen for any project that wants opinionated content-authoring CLI infra.
- **Health subsystem** — `scripts/health/` (105 files) with `checkers/`, `reporter.ts`, `runner.ts`, `scorer.ts`, `bundle.ts`, `history.ts`, `authority-cli.ts`. Provides `npm run health` / `health:snapshot` / `health:trend`. Substantially more than `science-tool health` offers. *Worth a future design pass* on whether a generic project-health framework belongs upstream.
- **Audit subsystem** — `scripts/audit/` (`coverage-prose.ts`, `coverage-visual-artifacts.ts`, `fidelity-distribution.ts`, `morphism-visual-pilot.ts`, `check-skill-exists.ts`). Drives milestone-gate / coverage scoring distinct from health.
- **Recon plans** — `doc/plans/recon/detail-page-parity-{behavior,dimgroup,facet,symmetry,variable}.md` as a parent-child plan family with `parent: plan:...` frontmatter. *Project-specific* model of pre-migration content inventories.
- **Specs sub-classification by entity link** — `related_questions`, `related_specs`, `related_interpretations`, `related_reports`, `related_tasks` (in `specs/*-design.md`). *Useful for ≥2 projects* — finer granularity than the single `related:` field used in `doc/`. Likely missing upstream support.
- **Curated must-have lists** — `specs/catalog-canonical-core.md` is a top-down "tier 1 / tier 2" list of canonical entities with `kind`, `rationale`, `evidence_refs`. *Project-specific* but a strong pattern for projects with a large entity catalog.
- **Long-form chapter prose with embedded entity blocks** — `content/prose/chapter/0X-*.md` (26 files, 102k+ lines) using `<!-- model: id -->` HTML-comment markers + inline classification tables + `**Key:** value` lines. *Project-specific*; a candidate for a future "structured prose" entity-model design pass.
- **Workflows-per-analysis-family** — `workflows/<analysis-family>/{Snakefile, config.yaml, ...}` × 9. *Useful for ≥2 projects* if any other project has multiple distinct pipelines.
- **Method-doc family** — `doc/guides/{formula-coverage,formula-structure-pipeline,improving-prose}.md` is a small how-to-author family separate from `doc/plans/` — *project-grown*, conceptually similar to a "method" or "playbook" family.

## 10. Candidate Upstream Changes

| Candidate | Evidence | Scope | Priority | Migration Cost | Notes |
| --- | --- | --- | --- | --- | --- |
| Parameterize `LOCAL_PROFILE` in canonical `validate.sh` (read from `science.yaml.knowledge_profiles.local`) | `validate.sh` lines 87, 115–117 vs `meta/validate.sh`; `science.yaml.knowledge_profiles.local: project_specific` | upstream | high | low | `mav-input`. Cleanest single fix; would let natural-systems pin canonical validator without losing semantics. |
| Bring `.env` sourcing into canonical `validate.sh` | `validate.sh` lines 12–19 (sources `.env` for `SCIENCE_TOOL_PATH`); same pattern likely needed by any non-monorepo downstream | upstream | medium | low | `mav-input`. Probably already shared; cross-check during synthesis. |
| Replace `ontologies` list-shape check with `knowledge_profiles.curated` list-shape check in canonical | `validate.sh` lines 153–155 vs canonical (5 lines); `science.yaml` shows `knowledge_profiles.curated: []` is the structurally important field | upstream | medium | low | `mav-input`. Confirm in synthesis whether `ontologies` list-shape check is still wanted. |
| Define a normative report-id-prefix policy (`report:` for `doc/reports/*.md`) and a validator check | 26 `id: doc:DATE-slug` vs 4 `id: report:DATE-slug` in `doc/reports/`; 1 file with no frontmatter (`2026-03-26-lens-model-mentions-audit.md`) | recurring | medium | low | Not solvable by MAV — requires entity-model rule + a `validate.sh` cross-check. |
| Tighten / canonicalize `status` enum (per-type, with explicit `partially-supported` slug); deprecate `complete` vs `completed` | 25 distinct `status` values; `complete`(76)/`completed`(1); `partially-answered`(13)/`partially-resolved`(1)/`partially supported`(1) | recurring | high | medium | Touches every entity family. Hypothesis-phase plan already partially addresses hypothesis statuses. |
| Resolve `topic` vs `concept` for `doc/topics/` files | `doc/topics/*.md` (14 files, type `concept`, id prefix `concept:`) vs `doc/background/topics/*.md` (1 file, type `topic`, id prefix `topic:`) | recurring | medium | low | Pick one; possibly two distinct kinds (research-topic vs domain-concept) need separate families. |
| Auto-archive done tasks from `tasks/active.md` to `tasks/done/YYYY-MM.md` | `tasks/active.md`: 114/232 entries with `status: done` (49%) — pre-existing condition, not domain-specific | recurring | medium | low | A `science-tool tasks archive` command would close this. |
| First-class entity-typed `related_questions` / `related_specs` / `related_interpretations` / `related_reports` / `related_tasks` fields | `specs/2026-04-22-morphism-generator-families-design.md` and 4 other specs use this shape | recurring | low | medium | Hypothesis: subset of projects want typed-link fields; finer-grained than `related: [...]`. Surface to synthesis as a candidate, not a P1. |
| First-class refactor-pair doc family (`desired_file_structure.md` + `files_to_remove.md`) | `doc/{guide-data-model-refresh,meta-model-refinement,project-organization-migration,provenance-derivation-redesign}/`; `doc/refactors/<topic>/files_to_remove.md`; matches `~/.claude/CLAUDE.md` global rule | project-specific | low | low | Hypothesis. Useful only if Science wants to model refactor-as-entity. |
| Datapackage profile registration (`science-research-package`) | 117 `datapackage.json` files under `public/research/` and `research/packages/`; canonical `research:` block extension | recurring | low | medium | Worth surfacing for synthesis; check if mm30 / cbioportal also use frictionless datapackages. |
| Allow non-markdown entity registries (`content/<family>/*.yml`) in the Science model | 234 `content/models/*.yml`, 281 `content/visuals/*.yml`, plus 8 more YAML registry families | project-specific | low | high | Hypothesis. Big design pass; defer to synthesis to confirm cross-project pressure. |
| Plan-family alignment between `doc/plans/`, `docs/plans/`, `docs/superpowers/{plans,specs}/` | `doc/plans/` (191) vs `docs/plans/` (8, no frontmatter) vs `docs/superpowers/plans/` (8) + `docs/superpowers/specs/` (28) | project-specific | medium | low | Likely accidental drift; confirm with `mm30` / `protein-landscape` in synthesis to see if `docs/superpowers/` is a global superpowers artifact. |
| Inline task `Result:` paragraphs should link to interpretation/report entities | `tasks/active.md` lines 9, 19, 29, 39, etc.; multiple `**Result:**` blocks with inline interpretive content | recurring | medium | medium | Not unique to natural-systems; tasks-as-narrative is a pattern. |
| Hold/separate `doc/inquiries/` family (vs typing them as `report` or `inquiry`) | `doc/inquiries/data-fitting-pilot-review.md` typed `report`; `doc/inquiries/fidelity-causal-dag.md` typed `inquiry` (with `inquiry_type: causal`) | project-specific | low | low | Hypothesis; small surface. Note in synthesis. |
| Validator id-prefix conformance check | All evidence above (report id-prefix drift, plan/doc id mixing, `interpretation:2026-01-01-test-*` test ids in plan frontmatter) | recurring | medium | low | A simple per-type id-prefix grammar check in `validate.sh`. |

---

### Auditor notes / surprises (for the Phase-1 / synthesis pass)

1. **Inventory `validate.structural_diff: []` is wrong.** Real `diff` shows ~80 lines / 14 blocks of meaningful local modification (env loading, profile parameterization, ontologies → curated, graph-audit error handling). The v0 diff only checks section header presence/order; it misses content drift. Phase-1 inventory bug or known limitation? Tooling-notes.md does not currently flag it. Recommendation: either record per-section sha256 or add a `content_diff_lines` count.
2. **Embedded-metadata false positives are dominant in long plans.** Of 154 flagged files, the 26 chapter-prose files are the genuinely interesting hits; the rest are mostly long plans where YAML-shaped key-value runs are bullet-points discussing fields. The `tooling-notes.md` "Embedded-metadata heuristic is purely lexical" caveat is real and matters for prioritization.
3. **`question:q61-...` duplicate id in `doc/topics/variational-unity.md`** is a genuine accidental drift — that file is a topic with a copied question id. Candidate for a one-line frontmatter fix in a follow-up cleanup PR.
4. **`interpretation:2026-01-01-test-*` ids inside `doc/plans/2026-04-08-claim-evidence-population-plan.md`** look like test-fixture ids in production prose. Verify whether they are intentional plan content or accidentally checked-in placeholders.
5. **Hypothesis `phase: candidate` is correctly used (h07 only)**, consistent with the in-progress `2026-04-25-hypothesis-phase.md` plan.
